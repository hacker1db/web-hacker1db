---
title: "Test Post from @post Directory"
date: "2024-01-15"
author: "Test Author"
tags: ["test", "example"]
subtitle: "This is a test post from the @post directory"
---

# Test Post

This is a test post to verify that the @post directory is being picked up by the posts system.

## Features

- This post is in the `@post/` directory
- It should be detected by the current posts library
- It demonstrates that the directory structure works

## Code Example

```javascript
console.log("Hello from @post directory!");
```

This post verifies that posts in the `@post/` directory are being processed correctly.